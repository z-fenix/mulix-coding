# T004 — Brief: confirm backward-compat against T002

Task: Confirm T003 passes against the T002 implementation (no separate
loader change expected, since `omitempty` + zero-value string already
round-trips correctly — this task exists to prove that assumption rather
than to add code).

Context: verification-only task; its report records the re-run result.
Dispatched to: subagent-2.
